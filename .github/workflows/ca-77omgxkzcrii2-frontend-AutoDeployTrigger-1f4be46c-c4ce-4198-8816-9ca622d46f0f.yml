name: Trigger auto deployment for ca-77omgxkzcrii2-frontend

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/ca-77omgxkzcrii2-frontend-AutoDeployTrigger-1f4be46c-c4ce-4198-8816-9ca622d46f0f.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CA77OMGXKZCRII2FRONTEND_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CA77OMGXKZCRII2FRONTEND_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CA77OMGXKZCRII2FRONTEND_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: cr77omgxkzcrii2.azurecr.io
          registryUsername: ${{ secrets.CA77OMGXKZCRII2FRONTEND_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CA77OMGXKZCRII2FRONTEND_REGISTRY_PASSWORD }}
          containerAppName: ca-77omgxkzcrii2-frontend
          resourceGroup: rag_resourcegroup
          imageToBuild: cr77omgxkzcrii2.azurecr.io/ca-77omgxkzcrii2-frontend:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


